# PLAN

## Steps

1. **Stub functions** — declare interfaces + types before implementing.
2. **Write tests** — describe expected behavior including edge cases.
3. **Implement** — make tests pass.
4. **Run typecheck + lint + test** — `npm run typecheck && npm run lint && npm test`.
5. **PR description** — write summary, test plan, rollback plan.

## Approval

`[ ] User confirmed`
