# PLAN

> Generated by Claude during the Plan phase from SCOPE.md. The CLI requires PLAN.md to exist with at least one numbered step before `spear execute` will run.

## Steps

1. **Generate `workspace/deck/build.js`** — pptxgenjs source covering all slides with brand tokens (Georgia + Arial, midnight + glacier palette), helpers (eyebrow / headline / subhead / footer / statTile / card), and one slide function per narrative section.
   - Produces: `workspace/deck/build.js`
   - Touches: reads SCOPE.md, narrative inputs
   - Success: file exists, exports nothing (it's a script), references all brand tokens

2. **Generate illustrations via gpt-image-2** — for each slide that needs a hero image, write a generation script with explicit composition + palette prompts. Aspect ratio matches placement.
   - Produces: `workspace/deck/images/*.png`
   - Tool: gpt-image-2 (size 1024×1024 / 1920×768 / etc.)

3. **Crop team photos to circles** — if SCOPE mentions team headshots, run `python3 crop_circles.py` to produce circular PNGs.
   - Produces: `workspace/deck/logos/brand/*_circle.png`

4. **Build pptx** — `cd workspace/deck && node build.js → output/deck.pptx`.

5. **Render to JPEGs** — LibreOffice converts pptx → pdf → jpegs in `workspace/qa/v-NN.jpg`.

## Approval

`[ ] User confirmed`

(Mark `[x] User confirmed` to unblock `spear execute`.)
